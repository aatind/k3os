ssh_authorized_keys:
- github:aatind
hostname: k3os-public-master1
boot_cmd:
- mkdir -p /var/lib/nfs/sm
- rc-update add rpc.statd
- rc-update add nfs

k3os:
  
  k3sArgs:
    - server
    - --cluster-init
    - --tls-san=k3s-public-master1.systechit.local
    - --flannel-backend=vxlan
